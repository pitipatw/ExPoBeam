function strip(dy::Float64)
    check file, if there's a file, laod it? 

    or use interpolation? 

    let's use interpolation. 

    end

    return output y pos and strips values, for interpolation.
end


function numerical integration
